// Boats actions
export { getBoats, createBoat, updateBoat, deleteBoat } from './boats'

// Clients actions
export { 
  getClients, 
  createClient, 
  updateClient, 
  deleteClient,
  updateClientReminder,
  deleteClientReminder 
} from './clients'

// Dashboard actions
export { getDashboardStats, getReminders } from './dashboard'
